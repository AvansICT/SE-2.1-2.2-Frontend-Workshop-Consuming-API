// TODO: import the generated functions after running `npm run generate`
import { getRandomFact, getTodayFact, getFactById } from "./generated/uselessfacts";

document.getElementById("btn-random")!.addEventListener("click", async () => {
  // TODO: call getRandomFact() and display the result in #output-random

});
